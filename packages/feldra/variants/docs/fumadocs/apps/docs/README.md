# Documentation

This generated project includes a Fumadocs documentation app. From the repository root:

```sh
npm run docs:dev
npm run docs:build
```

Open http://localhost:4321. Edit MDX in `apps/docs/content/docs`. The Feldra monorepo's own product docs stay on Blume; this app documents the generated SaaS.
