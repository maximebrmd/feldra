---
"feldra": minor
---

Add Auth.js (NextAuth v5) as an authentication choice alongside Better Auth and Clerk. Bundle the Auth.js overlay and a resolved dependency lockfile, remove unused Better Auth/Resend tooling from Auth.js projects, keep server authorization, and document GitHub OAuth setup.
